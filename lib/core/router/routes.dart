/// 路由路径常量。新增路由时在此集中维护。
abstract final class Routes {
  static const splash = '/';
  // 后续按 feature 扩展，例如：
  // static const weatherHome = '/weather';
  // static const outfitDetail = '/outfit/:id';
}
